version: "3.8"

services:
  web:
    image: viraj231/virajweb:latest
    container_name: virajweb_nginx
    restart: always
    ports:
      - "80:80"
    networks:
      - gateway_net

networks:
  gateway_net:
    external: true
